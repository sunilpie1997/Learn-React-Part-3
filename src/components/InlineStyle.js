import React from 'react';

const InlineStyle=(props)=>{

    const heading={color:'blue',textAlign:'center'}

    return(
        <div>
           
            <h2 style={heading}>Inline Styling</h2>
        </div>
    );
}

export default InlineStyle;